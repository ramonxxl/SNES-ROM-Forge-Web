{
  "name": "snes-rom-forge-web",
  "version": "1.0.0",
  "description": "Port client-side (sem servidor) do SNES ROM Forge",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
